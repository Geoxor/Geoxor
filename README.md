![Waifus](https://count.getloli.com/get/@geoxor?theme=rule34)

```ts
class Geoxor extends Human {
  constructor() {
    super();
    this.name =        (⚠) => "Geoxor"
    this.birth =       (s) => "27/03/2001"
    this.pronouns =    (o) => [ "troll", "trolley" ];
    this.code =        (f) => [ Typescript, Vue, Windi, Java, Rust ];
    this.IDE =         (t) => "VS Code";
    this.IDETheme =    (w) => [ "Material Icons Theme", "Material Ocean Dark High Contrast" ];
    this.codeStyle =   (a) => "object-orianted";
    this.lifeEndings = (r) => "lf";
    this.formatter =   (e) => prettier";
    this.projects = {
      ...new Xornet,
      ...new Taku,
      ...new Sakuria,
    };
  }
}
```
